{TESTES}
inicio
    varinicio
        literal A;
        real x, y, z;
        inteiro B, D, E, C;
    varfim;
    escreva "Digite B: ";
    leia B;
    escreva B;
    B <- x + 1.0;     {ERRO 1}
    se (B > 2) entao
        se (B <= 4) entao
            escreva "B esta entre 2 e 4";
        fimse
    fimse
    C <- 5.0;       {ERRO 2}
    repita (B < 5)
        C <- B + 2;
        escreva C;
        A <- 5;     {ERRO 3}
        escreva A;
        B <- B + 1;
    fimrepita
    escreva "Digite A: ";
    leia A;
fim